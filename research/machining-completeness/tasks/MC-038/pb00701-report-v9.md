# PB-007-01 v9 — exact monotone single-harmonic ratio reduction

## RAG status

**AMBER — a genuinely non-common coupled analytic subgrammar now terminates exactly; PB-007-01 remains OPEN.** Corrective issue #180 extends the v7/v8 source-bound analytic route without rewriting historical v3/v6/v7/v8 evidence. The frozen 26-operation denominator and protected source/audio/provenance semantics remain unchanged. MC-B and MC-1 remain `NOT_ESTABLISHED`.

## Exact route established

On an exact v7 knot span, v9 admits exactly one positive harmonic with nonzero rational-polynomial cosine and sine modulation,

`F(s) = A(s) cos(h theta(s)) + B(s) sin(h theta(s))`,

only when exact rational-polynomial GCD proves `A` and `B` coprime. This deliberately excludes the common-factor family already owned by v8. MC-032 Sturm authority must also prove that `B` is nonzero on the entire closed local source span.

On every chart where `tan(h theta)` is finite,

`F(s)=0` iff `tan(pi*u(s)) = r(s)`,

with `u(s)=2*h*theta_turn(s)` and `r(s)=-A(s)/B(s)`. The exact ratio derivative is

`r'(s) = (A(s) B'(s) - A'(s) B(s)) / B(s)^2`.

Because `B^2` is strictly positive, MC-032 exact polynomial root/sign authority decides the derivative direction for the bounded route. If `u` increases, v9 requires `r` to be nonincreasing; if `u` decreases, it requires `r` to be nondecreasing. An identically zero ratio derivative is also allowed. Under those conditions

`H'(s)=pi*u'(s)*sec^2(pi*u(s)) - r'(s)`

has the fixed strict sign of `u'` on each pole-free chart. This proof uses only the positivity of `pi` and `sec^2`; it does not use a floating approximation or rational numerical bound for `pi`.

## Exact chart and endpoint handling

The local source span is split exactly wherever `u(s)=n+1/2`, the poles of `tan(pi*u)`. Those source cuts are rational because the admitted phase law is rational affine. One-sided values of `H` at a pole are symbolic `+infinity` / `-infinity`, determined by the exact phase direction; they are never approximated numerically.

At finite rational source boundaries, `r(s)` is rational and `tan(pi*u(s))` is represented by the existing v6 exact rational-turn algebraic endpoint object. Equality/sign of `tan(pi*u)-r` is decided by v6 polynomial GCD plus isolating-interval refinement. Hence each pole-free chart has exactly zero or one open event according to its exact boundary signs. Every admitted open event is simple because `H'` is strictly nonzero there and `B*cos(h theta)` is nonzero away from the ratio poles.

At a tangent pole, the original expression is not singular: `cos(h theta)=0` and `sin(h theta)=+/-1`. Since `B` is independently certified nonzero, the original event is exactly nonzero; the pole is only a chart boundary. External source endpoint equalities are deliberately left blocked in v9 rather than inventing coupled endpoint multiplicity evidence.

## Adversarial boundary

The verifier covers a genuinely non-common single root, a no-root chart, decreasing phase, exact `+/-1/1000000` endpoint neighbours, multiple tangent-pole charts, external endpoint equality, a source zero of `B`, ratio-derivative sign change, multiple harmonics, harmonic zero, v8 common-factor ownership, zero phase rate, parameter mismatch, binary-float/epsilon authority, forged ratio/monotonicity fields, resource-refusal laundering, historical-evidence mutation, denominator shrinkage and false MC-B promotion.

Unsupported cases fail closed. In particular, v9 does not use finite sampling, tolerance, a subdivision cap or timeout as a truth value. It does not certify multiple active harmonics, a ratio denominator with source zeros, a nonmonotone ratio, unsupported endpoint multiplicity, or the remaining general polynomial/trigonometric-exponential zero problem.

## Remaining blocker

PB-007-01 therefore remains **OPEN** for non-common coupled rational-polynomial harmonic modulation outside this single-harmonic zero-free opposite-monotone ratio family, including multi-harmonic and nonmonotone cases. This is not an impossibility theorem. PO-04, PO-05 and PO-08 remain OPEN; PB-007-02 remains dependent on PB-007-01; PB-007-03 remains OPEN; PB-007-04 remains `OPEN_PROPAGATED`; MC-B and MC-1 remain `NOT_ESTABLISHED`.

No native, paid, production or expensive campaign is authorized. Source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume, cutter/holder access, durable body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics are preserved.
