# PB-007-01 v33 — pointwise separator integration result

## Decision

V33 consumes the qualified v32 source-owned rational-separator foundation in the preserved v30 projective/residual proof without weakening any residual or event contract. The integration is exact and finitely terminating, but this package does **not** promote new executable event-classifier authority: the first targeted v31 witness and a stronger exact common-factor control remain blocked by the preserved selected-harmonic amplitude-derivative residual envelope. PB-007-01 therefore remains OPEN and MC-B / MC-1 remain `NOT_ESTABLISHED`.

## Exact integration

For a selected mixed harmonic, v33 derives the canonical dominant/transverse source polynomials and dominant sign, invokes the v32 source-owned algebraic critical-value / rational-separator constructor, verifies its source binding, then passes only the certified rational separator into the unchanged v30 finite sign-orthant proof. Both selected-harmonic amplitude derivatives, harmonic-0, and every non-anchor derivative remain residual. Exact equality, failed binding, unresolved algebraic ordering, failed orthants and resource refusal remain fail-closed.

The construction uses no binary floating point, numerical trigonometry, sampling, epsilon/tolerance, arbitrary rational denominator cap, arbitrary subdivision depth, timeout, or resource budget as truth authority. V32 finite Sturm/root-isolation/resultant ordering and closed-form separator synthesis are followed by the finite v30 exact rational-polynomial orthant enumeration.

## Targeted diagnostics

The v31 diagnostic `D(s)=1+2s`, `T(s)=2+3s` now receives a source-owned rational separator from v32 without trusting the historical diagnostic `m=5/12`. However, preserving the full derivative contract means the selected-harmonic amplitude derivatives `D'=2` and `T'=3` remain active residuals, and the v30 orthant proof still fails. This is a real integration result but not a new event certificate.

A second exact control makes the next theorem boundary clearer. Let

`G(s)=(1+3s/4)^2`, with `C(s)=S(s)=G(s)`.

The pointwise projective ratio is exactly one, while v30's separate global Bernstein floor/ceiling ratio is `16/49`, strictly below `tan(pi/8)=sqrt(2)-1`. V32 exactly cancels the common source polynomial factor and synthesizes a source-owned separator, but the unchanged independent selected-harmonic amplitude-derivative envelope remains blocking. This does not prove a general impossibility theorem; it identifies the next conservative proof layer on deterministic exact controls.

## Next repair

The next dependency-ready PB-007-01 repair is an exact **phase-correlated selected-harmonic amplitude/phase derivative certificate**. The bounded first family should exploit source-owned proportional/common-factor quadratures and an exact diagonal rational-turn phase cell so that `C' cos + S' sin` is bounded jointly with the selected phase projection instead of being replaced by the independent `|C'|+|S'|` envelope. All non-anchor residuals, endpoint/root/multiplicity authority, refusal semantics and complete historical precedence must remain intact.

The machining-domain denominator remains frozen at 26 operations. Protected source/audio/provenance, canonical journal, exact time/path/phase, source uncertainty, positive-volume material, cutter/holder, durable body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics are unchanged. No native, paid, production or expensive campaign was run or authorized.
