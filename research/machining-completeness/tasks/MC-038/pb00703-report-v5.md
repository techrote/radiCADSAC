# PB-007-03 v5 — exact rational parametric boundary-source construction

Corrective issue: #174. Source baseline: `a4ca147c54827ef30f1192a4a29b72980a91722c`.

## Result

This pass broadens the bounded constructive source authority established by PB-007-03 v4. The v4 exact-rational semialgebraic solid codec remains byte-for-byte historical evidence. The new v5 route adds an exact nominal tensor-product B-spline/NURBS-style **boundary patch** language with exact rational knots, control points, positive weights, active parameter domains, source/patch identities, roles, units, frame, parameter identities and source uncertainty.

Evaluation uses Cox-de Boor semantics over exact `Fraction` arithmetic. Repeated knots and zero-width knot spans are handled by exact zero-denominator branch omission, not epsilon. A clamped active-domain high endpoint maps exactly to the final basis function. Canonical hashing binds every authority-bearing field and normalizes rational spellings.

The weighted control example evaluates at `(u,v)=(1/2,1/2)` to exactly `(3/5,3/5,2/5)`. Exact `±1/1000000` parameter neighbours remain distinct. Binary floats are not exact authority.

## Boundary of the construction

This is deliberately a boundary-source construction, not a solid-membership proof. A finite collection of exact patches does not, by itself, certify trims, seam agreement, watertightness, consistent shell orientation, positive-volume interior, or inside/outside membership. `request_solid_membership` therefore fails closed with `PB-007-03`, even if a caller supplies a self-asserted `watertight` or kernel-valid flag. Opaque STEP/NURBS/B-rep importer success remains non-authoritative.

A nonzero source uncertainty produces `UNCERTIFIED_SOURCE`; nominal exact evaluation cannot launder measured/import uncertainty into exact material truth. Cutter and holder/access semantics remain separate programme authority and are not collapsed by this boundary codec.

## Adversarial controls

The deterministic verifier covers planar/bilinear and genuinely rational weighted patches; exact `1/1000000` neighbours; repeated knots and exact knot endpoints; malformed cardinality; ragged control nets; nonpositive weights; binary-float knots/control/weights/parameters; parameter-domain mismatch; role/units/frame/parameter-identity drift; unsupported trims; fake shell closure; opaque STEP/NURBS/kernel success; source-uncertainty laundering; denominator shrinkage; historical-v4 mutation; resource/native execution promotion; and false MC-B promotion.

## Programme effect

`PB-007-03` **remains open**. The residual obligation is an unconditional finite exact source-solid lowering/certificate route for every admitted imported-stock/arbitrary form/undercut source, including independently qualified trim, closed oriented shell and volumetric membership authority. This result is not an impossibility theorem.

`PB-007-01` remains OPEN, `PB-007-02` remains OPEN and dependent on PB-007-01, `PB-007-04` remains OPEN_PROPAGATED, and PO-02/PO-05 remain OPEN. MC-B and MC-1 remain `NOT_ESTABLISHED`.

The frozen 26-operation denominator is unchanged. Historical source/audio/provenance, canonical-journal semantics, source uncertainty, positive-volume material, holder/access separation, durable body/lineage, refusal/`UNCERTIFIED`, and conventional STEP output semantics are preserved. No native, paid, production or expensive campaign ran.
