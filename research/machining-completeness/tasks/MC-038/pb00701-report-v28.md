# PB-007-01 v28 — exact algebraic rational-turn phase-cell projection certificate

## Decision

PB-007-01 **remains OPEN**. MC-B and MC-1 remain `NOT_ESTABLISHED`; the frozen machining-domain denominator remains 26 operations. V28 adds one exact sufficient route for residual multi-harmonic spans still blocked after complete v8–v27 authority. It does not alter source/audio/provenance or downstream semantics.

## Exact phase-cell authority

The new route uses rational-turn cells whose endpoint trigonometric values are already algebraic. On COS-dominant phase cells `[-1/12,1/12]+k` and `[5/12,7/12]+k`, `|cos| >= sqrt(3)/2 > 6/7` and `|sin| <= 1/2`. On the symmetric SIN-dominant cells `[1/6,1/3]+k` and `[2/3,5/6]+k`, `|sin| >= sqrt(3)/2 > 6/7` and `|cos| <= 1/2`. The only irrational comparison needed by the polynomial certificate is eliminated exactly: `sqrt(3)/2 > 6/7` iff `3/4 > 36/49`, i.e. `147 > 144`.

For a selected fixed-sign source component `D`, transverse component `T`, and retained residual polynomials `R_i`, v28 proves every finite sign orthant of

`6|hr|((6/7) sign(D)D - (1/2) sigma_T T) - sum sigma_i R_i`

strictly positive on closed `[0,1]` using MC-032 exact endpoint/Sturm authority. Together with `2*pi > 6`, this proves a fixed nonzero complete derivative. Selected-harmonic amplitude derivatives, harmonic-0 derivative, and all non-anchor derivative channels remain explicit residuals.

## Materially broader acceptance family

The deterministic acceptance source uses `S_1=1`, `C_1=3/5`, harmonic-0 constant `-4/5`, a tiny active `C_2=1/10000`, and phase `0 -> 1/12` turn. Historical v26 cone authority fails because `S/2=1/2 <= 3/5=|C|`; v27 has no interior historical sector cut that can repair that child. V28 instead proves `6/7 - (1/2)(3/5)=39/70 > 0` before residual allowance and certifies the event with exact endpoint/root semantics.

The adversarial boundary covers exact phase-cell endpoints and `±1/1000000` neighbours, exact projection equality and neighbours, exact residual-L1 equality and neighbours, positive/negative phase rates, S- and C-dominant orientations, positive/negative phase quadrants, unsupported wider cells, forged algebraic/cell/separation/root metadata, binary floats, source-coordinate mismatch, exact resource refusal, v8–v27 precedence, frozen 26-operation coverage, and false MC-B promotion. The complete v27 verifier is rerun, preserving source-knot coincidence and internal certificate-cut root/multiplicity composition.

## Residual blocker / next routing

PB-007-01 remains OPEN after v28. Residual spans include phase intervals not contained in the finite supported rational-turn algebraic cells and coupled projections for which current exact rational/algebraic authority does not yield a strict uniform polynomial separation. This is not an impossibility theorem.

The next repair should widen exact phase projection authority only if it can remain finitely terminating and exact—for example by deriving additional algebraic rational-turn cells or an exact projective comparison that reduces to polynomial sign authority. If that requires genuinely new transcendental minimization/zero theory or non-representable source cuts, record that theorem boundary rather than substituting numerical approximation.

Protected source/audio/provenance, canonical journal, exact time/path/phase, source uncertainty, positive-volume material, cutter/holder, durable body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain unchanged. No native, paid, production, or expensive campaign ran.
