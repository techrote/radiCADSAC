# PB-007-01 v41 — exact orientation-root partition composition

## Decision

PB-007-01 remains **OPEN**. MC-B and MC-1 remain `NOT_ESTABLISHED`; the frozen machining-domain denominator remains 26 operations. This package adds only a bounded exact sufficient route after complete v40 authority.

## Repair

For residual exact rational-polynomial spans, v41 derives rotated coordinates `A=(C+S)/2` and `B=(C-S)/2` from source coefficients, proves a finite exact set of rational interior orientation roots complete by an independent Sturm distinct-root count, unions those roots with historical v27 exact phase-sector cuts, and reparameterizes every child exactly. Every child is rerun through complete v40 authority; no child certificate is trusted from the caller.

The historical v27 event composer is reused directly. Internal proof cuts contribute no physical root unless both adjacent exact endpoint relations report the same physical equality and compatible explicit multiplicity. Any disagreement, blocked child, or exact resource refusal propagates fail-closed.

The adversarial suite covers rational-root order/multiplicity, endpoint roots, repeated roots, coincident cut deduplication, an exact complete-v40 residual acceptance family, reverse phase rate, forged partition/root/child evidence, historical v27/v40 precedence, event-neutral internal cuts, semantic mismatch, resource refusal, and the explicit irrational-algebraic root boundary.

## Residual boundary

The exact child representation remains rational. A Sturm-proven interior orientation root not exactly representable as a rational source coordinate is an explicit theorem/representation blocker, not a license for approximate root ordering or subdivision. Full PB-007-01 therefore remains open.

Protected source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics are unchanged. No native, paid, production, or expensive campaign is authorized.
