# PB-007-01 v41 — v42-aware exact orientation-root partition composition

## Decision

PB-007-01 remains **OPEN**. MC-B and MC-1 remain `NOT_ESTABLISHED`; the frozen machining-domain denominator remains 26 operations. This package resumes #245 only after verified v42 / #247 / PR #248.

## Corrected authority

The closed #246 attempt established that cutting at an exact `A=0` or `B=0` orientation root does not make either adjacent **closed** child strict-admissible to v39/v40. Verified v42 adds the missing closed-span derivative bridge. V41 composition therefore runs complete current authority through v42 first and only partitions residual blocked lowered spans.

For each exact child, complete v42 authority is rerun independently. Historical precedence remains intact: earlier routes retain ownership where they already decide a child; the v42 transition bridge is used only where its exact premises hold. A zero-adjacent child is never promoted merely because a proof cut exists.

## Exact partition/composition

Source-owned `A=(C+S)/2` and `B=(C-S)/2` are regenerated exactly. Rational interior orientation roots are enumerated by the rational-root theorem only when an independent exact Sturm distinct-root count proves completeness. Multiplicity is exact polynomial division. Orientation roots are unioned with historical v27 exact phase-sector cuts, sorted and deduplicated exactly, and every child uses the existing exact v27 rational reparameterization.

The historical v27 event composer remains the authority for parent root/event composition. A proof cut contributes no physical root unless both adjacent exact endpoint relations establish the same equality with compatible multiplicity. Mismatch, unresolved child authority, or resource refusal propagates fail-closed.

## Acceptance requirement

The focused suite searches a bounded exact two-harmonic family for a source that complete v42 still blocks, but exact orientation-root partitioning makes certifiable by independent child authorities. Acceptance additionally requires an actual v42 transition-handoff child and at least one differently owned child, so the result demonstrates composition beyond v42 itself rather than replaying the v42 acceptance fixture. The reverse exact phase law must also remain predecessor-blocked and composition-certified.

## Residual boundary

The child representation remains rational. A Sturm-proven interior orientation root not exactly representable as a rational source coordinate remains an explicit theorem/representation blocker. No approximate root ordering, epsilon cut, sampling, adaptive refinement, timeout, or subdivision cap can become correctness authority.

Protected source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics are unchanged. No native, paid, production, or expensive campaign is authorized.
