# PB-007-01 v36 — exact phase-correlated varying-ratio shared-factor derivative anchor

Status: corrective authority established for the bounded v36 family; **PB-007-01 remains open**. MC-B and MC-1 remain `NOT_ESTABLISHED`.

## Scope

V36 extends verified v35 constant proportionality to exact source-owned nonconstant polynomial proportionality

`C_h(s)=lambda(s)G(s)`, `S_h(s)=G(s)`

where `lambda(s)` is a nonconstant exact rational polynomial and `G(s)` is an exact rational polynomial of one strict sign on the complete lowered source span. `lambda` is never caller authority: v36 derives it by exact rational-polynomial division of source-owned `C_h` by `S_h=G`, requires a zero remainder, and regenerates every coefficient of `C_h=lambda G` exactly.

Complete v8-v30 executable authority, v34, and v35 precedence run first. V33 theorem-boundary evidence remains pinned. V36 only attempts residual blocked spans.

## Exact correlated derivative

On the preserved exact diagonal phase cell

`-3/16+k/2 <= h*phi <= -1/16+k/2`,

write `X=cos(theta)+sin(theta)` and `Y=cos(theta)-sin(theta)`. Existing exact rational authority gives

- `|X| < U = 99/182`;
- `Y` has fixed sign and `|Y| > L = 2856/2197`;
- `|Y| < W = 99/70`;
- `|cos(theta)| <= 1`;
- `2*pi > 6`.

For `A(s)=(lambda(s)+1)/2` and `B(s)=(lambda(s)-1)/2`, the complete selected-harmonic derivative is exactly

`G'*(A*X+B*Y) + lambda'*G*cos(theta) + 2*pi*h*r*G*(A*Y-B*X)`.

The new `lambda'*G*cos(theta)` contribution is mandatory live selected-harmonic evidence. It is neither dropped nor accepted from caller metadata.

MC-032 endpoint/Sturm authority proves `A(s)>0`. To avoid approximate root partitioning of `B`, v36 proves both finite phase-gap polynomials

`P_sigma(s)=A(s)*L-sigma_B*B(s)*U > 0`, for `sigma_B in {-1,+1}`.

For both `sigma_B` values and every exact sign orthant over `A*G'`, `B*G'`, `lambda'*G`, and every non-anchor derivative residual `R_i`, v36 requires

`6*|h*r|*sign(G)*G*P_sigma - sigma_A*U*(A*G') - sigma_T*W*(B*G') - sigma_lambda*(lambda'*G) - sum_i sigma_i*R_i > 0`.

Every inequality is an exact rational polynomial discharged by MC-032 closed-interval endpoint/Sturm strict positivity. Equality fails closed. Exact resource refusal remains non-truth. No sampling, binary-float authority, epsilon/tolerance, numerical trigonometry, approximate root ordering, arbitrary subdivision/refinement cap, timeout, denominator cap, or resource budget is correctness authority.

## Acceptance source

The primary source uses `lambda(s)=9/10+s/100`, `G=(1+3s/4)^2`, `C_1=lambda G`, `S_1=G`, exact phase interval `[-3/16,-1/16]`, and a nonzero tiny `h=2` COS term so a non-anchor phase residual remains live. V35 blocks specifically because proportionality is nonconstant. V36 derives

- `A(s)=19/20+s/200`;
- `B(s)=-1/20+s/200`;
- `lambda'(s)=1/100`;
- `lambda'G=(1/100)G`;

and certifies the complete event while retaining both selected `G'` channels, both selected phase channels, the new ratio-derivative term, and every non-anchor residual.

## Adversarial boundary

Executable controls cover positive and negative phase rates, opposite diagonal projection-sign cells, exact phase-cell equality and signed rational `1/1000000` neighbours, exact `A(s)>0` endpoint equality and neighbours, exact pointwise phase-gap equality at `lambda(0)=-7751/18905` for one `sigma_B` and neighbours, strict complete-margin equality and neighbours, zero/open/left-endpoint/right-endpoint outcomes under preserved root/multiplicity authority, nonzero polynomial-division remainder, constant-ratio v35 ownership, zero/sign-changing `G`, unsupported cells, source-coordinate mismatch, forged ratio/common-factor/division/cell/bound/margin/root metadata, binary floats, and exact resource refusal.

Historical v30, v34, and v35 successes retain exact precedence. V33 blocker evidence remains pinned. The machining-domain denominator remains frozen at **26 operations** and false MC-B promotion is rejected.

## Programme effect

PB-007-01 remains open globally; PB-007-02 remains dependent; PB-007-03 remains open; PB-007-04 remains `OPEN_PROPAGATED`; PO-04/05/08 remain open. MC-B and MC-1 remain `NOT_ESTABLISHED`. No native, paid, production, or expensive campaign is authorized or run.

All protected source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain unchanged.

## Next repair frontier

The next bounded pre-gate extension should consider exact two-polynomial shared-factor quadratures `C_h=N(s)G(s)`, `S_h=D(s)G(s)` where both source-owned `N` and `D` vary and `C/S` need not itself be a polynomial. Any such route must retain `N'G` and `D'G` explicitly and use finite exact rational-polynomial/Sturm authority; if that cannot be made finite with existing theory, record the precise blocker rather than introducing approximation.
