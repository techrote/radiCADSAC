# PB-007-01 v35 — exact phase-correlated proportional common-factor derivative anchor

Status: corrective authority established for the bounded v35 family; **PB-007-01 remains open**. MC-B and MC-1 remain `NOT_ESTABLISHED`.

## Scope

V35 extends the verified v34 equal-quadrature construction to exact source-owned unequal proportional quadratures

`C_h(s)=lambda*G(s)`, `S_h(s)=G(s)`

for nonzero exact rational `lambda` and an exact rational polynomial `G` of one strict sign on the complete lowered source span. The scalar is never caller authority: it is derived from source coefficients and every coefficient identity is regenerated exactly.

Complete v8-v30 executable authority and v34 precedence run first. V35 only attempts residual blocked spans. V33 theorem-boundary evidence remains preserved.

## Exact decomposition

On the existing exact diagonal phase cell

`-3/16+k/2 <= h*phi <= -1/16+k/2`,

write `X=cos(theta)+sin(theta)` and `Y=cos(theta)-sin(theta)`. V34 already established exact rational bounds

- `|X| < U = 99/182`;
- `Y` has fixed sign and `|Y| > L = 2856/2197`;
- `|Y| < W = 99/70`;
- `2*pi > 6`.

For `A=(lambda+1)/2` and `B=(lambda-1)/2`, the selected harmonic derivative is exactly

`G'*(A*X+B*Y) + 2*pi*h*r*G*(A*Y-B*X)`.

The bounded family requires `A>0` and the exact strict phase-correlation gap

`P=A*L-|B|*U > 0`.

Then the phase term has the sign of `h*r*G*Y` and magnitude strictly greater than `6*|h*r|*P*|G|`. The selected amplitude part is bounded above by

`Q*|G'|`, where `Q=|A|*U+|B|*W`.

The complete sufficient margin is therefore

`6*|h*r|*P*|G(s)| - Q*|G'(s)| - sum_i |R_i(s)| > 0`.

Every non-anchor derivative contribution remains explicit in `R_i`. The selected harmonic's two amplitude derivative channels and two phase channels are consumed only through the exact correlated identity above; they are not independently counted or silently dropped. Finite exact rational-polynomial sign orthants plus MC-032 closed-interval endpoint/Sturm strict positivity discharge every absolute value. Equality fails closed. Exact resource refusal remains non-truth.

## Acceptance source

The primary source uses `G=(1+3s/4)^2`, `lambda=9/10`, `C_1=(9/10)G`, `S_1=G`, exact phase interval `[-3/16,-1/16]`, and a nonzero tiny `h=2` COS term so a non-anchor phase residual remains live. V34 blocks because `C_1 != S_1`; v35 derives

- `A=19/20`;
- `B=-1/20`;
- `P=148593/123032`;
- `Q=2673/4550`;

and certifies the complete event with both selected amplitude derivatives active only through the joint identity.

## Adversarial boundary

The executable tests cover positive and negative phase rates, opposite diagonal projection-sign cells, exact phase-cell equality and signed rational `1/1000000` neighbours, the exact phase-gap equality `P=0` at `lambda=-7751/18905` and signed rational neighbours, exact complete-margin equality and signed rational neighbours, zero/open/left-endpoint/right-endpoint outcomes under preserved exact root and multiplicity authority, malformed or nonconstant proportionality, a one-coefficient source perturbation, zero/sign-changing `G`, `A<=0`, unsupported phase cells, source-coordinate mismatch, forged lambda/common-factor/cell/bound/margin/root metadata, binary floats, and exact resource refusal.

Historical v30 and v34 successes retain exact precedence. V33 blocker evidence remains pinned. The machining-domain denominator remains frozen at **26 operations** and false MC-B promotion is rejected.

## Programme effect

PB-007-01 remains open globally; PB-007-02 remains dependent; PB-007-03 remains open; PB-007-04 remains `OPEN_PROPAGATED`; PO-04/05/08 remain open. MC-B and MC-1 remain `NOT_ESTABLISHED`. No native, paid, production, or expensive campaign is authorized or run.

All protected source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain unchanged.

## Next repair frontier

The next pre-gate repair should target a bounded source-owned varying-ratio/shared-factor family where `C_h/G` and `S_h/G` are not constant but their correlated selected-harmonic derivative can still be discharged with finite exact rational-polynomial/Sturm authority. Do not replace this with numerical trigonometry, sampling, tolerance, approximate root ordering, arbitrary refinement depth, timeout, denominator cap, or resource budget as truth authority.
