# PB-007-01 v27 — exact rational phase-sector partition report

Issue: #216  
Parent integration gate: MC-038 / #100  
Source baseline: `cad06a39b2ba28d3b560dff409940473f4a3c186`

## Result

PB-007-01 gains a bounded exact finite certificate-cut composition route. A whole residual exact-rational affine-phase span may remain blocked after v26 solely because one historical derivative certificate cannot cover the complete phase interval. v27 derives all relevant half-magnitude sector crossings from the source phase law, partitions only at those exact rational preimages, reparameterizes every child exactly, delegates each child to preserved v8–v26 authority, and composes the resulting exact event summaries.

The acceptance source is genuinely v26-blocked on the whole span and becomes decidable only after an exact `s=1/2` phase-sector certificate cut. The left child is owned by preserved v24 mixed-quadrature authority; the right child is owned by preserved v25/v26 component-cone authority. Their exact endpoint/event summaries compose to one simple open root.

PB-007-01 remains OPEN. PB-007-02 remains dependent; PB-007-03 remains OPEN; PB-007-04 remains `OPEN_PROPAGATED`; PO-04/05/08 remain OPEN; MC-B and MC-1 remain `NOT_ESTABLISHED`.

## Source-derived partition

For each source-derived mixed candidate harmonic, the existing exact SIN/COS half-magnitude sector endpoints are rational turns. With exact affine phase `phi(s)=a+r*s`, every crossing has exact rational source coordinate

`s=(boundary/h-a)/r`.

Only strict interior cuts are admitted. External endpoint crossings do not create zero-width children. Coincident rational cuts collapse exactly while preserving provenance for every harmonic/boundary family that generated them.

There is no adaptive refinement. The construction terminates because the harmonic set and sector-boundary residue set are finite.

## Exact reparameterization

Every child `[a,b]` uses the exact substitution `s=a+(b-a)u`. Polynomial coefficients remain rational, the child phase law remains rational affine, and the shared source parameter identity is retained. Each child is re-encoded as a single exact clamped rational B-spline solely to feed the established classifier interface.

No source sampling, binary-float reconstruction, epsilon shift or tolerance inflation is used.

## Child decision and fail-closed behavior

The complete historical v8–v26 classifier owns each child. v27 accepts the parent only if every child is `CERTIFIED` and exposes exact endpoint relation, root-count and multiplicity evidence suitable for composition.

One blocked child keeps the parent blocked. Semantic blockers remain semantic blockers. Exact resource refusal propagates as refusal and is never converted to a mathematical result.

Caller-supplied partition, sector, child-certificate, root-count and deduplication metadata is stripped before analysis and cannot authorize a result.

## Root/event composition

At every internal cut, adjacent child endpoint relations must match exactly. A shared nonzero relation is not an event. A shared `ZERO` relation is deduplicated to one parent-open root only when both children prove endpoint multiplicity one. External endpoint roots remain external and retain their exact multiplicity records.

The adversarial suite includes a synthetic exact internal-cut root specifically to prevent double counting or multiplicity invention.

## Protected semantics

v27 changes only PB-007-01 analytic decision authority. It does not alter source/audio/provenance, canonical operation-journal meaning, exact time/path/phase semantics, source uncertainty, positive-volume material, cutter/holder access, durable bodies/lineage, refusal/`UNCERTIFIED`, or conventional STEP requirements.

The machining-domain denominator remains frozen at 26 operations. No native campaign, paid campaign, production bootstrap or expensive execution is authorized.

## Residual blocker

General coupled analytic zero isolation remains unresolved after v27. Remaining spans are those for which exact phase-cell normalization still leaves no historical factor, ratio, monotonicity, phase-anchor or component-cone proof route. This is an open theorem branch, not evidence that MC-B is established and not an impossibility theorem.
