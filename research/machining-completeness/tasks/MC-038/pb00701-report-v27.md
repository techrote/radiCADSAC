# PB-007-01 v27 — exact finite rational phase-sector partition composition

## Decision

PB-007-01 **remains OPEN**. MC-B and MC-1 remain `NOT_ESTABLISHED`; the frozen denominator remains 26 operations. This package adds a sufficient exact route for residual coupled spans that are blocked by preserved v8–v26 whole-span authority only because an affine rational harmonic phase traverses historical half-magnitude SIN/COS sector boundaries, while every exact certificate child is decidable under preserved authority.

No source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder access, durable body/lineage, refusal/`UNCERTIFIED`, or conventional STEP semantics are weakened.

## Exact certificate cuts

For every active positive harmonic `h`, v27 forms the exact harmonic phase

`theta_h(s) = h (offset + rate s)`

and solves it against the existing half-magnitude boundary residues. COS uses `{1/6,1/3,2/3,5/6}+Z`; SIN uses `{1/12,5/12,7/12,11/12}+Z`. Since the phase law and every boundary are rational, every crossing source parameter is rational. Only cuts strictly inside `(0,1)` are retained. Coincident cuts from different harmonics or boundary families are deduplicated exactly while all causes are retained.

This is a finite algebraic enumeration. There is no sampling, epsilon/tolerance, numerical trigonometry, approximate root ordering, adaptive subdivision, arbitrary refinement depth, timeout, or resource refusal used as truth authority.

## Exact child reparameterization

For a child `[a,b]`, each source polynomial `p(s)=sum_k p_k s^k` is transformed under `s=a+(b-a)u` as

`q_j = sum_{k>=j} p_k binom(k,j) a^(k-j) (b-a)^j`.

The child phase law is exactly `offset_child=offset+rate*a`, `rate_child=rate*(b-a)`. The shared source parameter identity is preserved together with the exact parent/global interval map. Each child is encoded as a single rational clamped Bezier span and passed back through the **complete v8–v26 classifier**. Historical route precedence therefore remains intact; v27 cannot bypass or reinterpret an earlier certificate.

## Exact event composition

Every child must be `CERTIFIED`. A blocked child, semantic blocker, or exact resource refusal propagates fail-closed. The composer requires an exact child-open root count and independently evaluates each child endpoint using the established rational-turn/tangent-half endpoint authority.

Adjacent child endpoint relations at an internal certificate cut must agree exactly. If both report `ZERO`, the cut represents one global open root, not two child endpoint roots. It is counted exactly once only when both children expose the same explicit endpoint multiplicity. Missing or conflicting multiplicity is a PB-007-01 composition blocker. Multiplicity is never inferred from a root count alone.

External endpoint relations and multiplicities remain the established source endpoint semantics. The certificate cuts themselves are not approximate event boundaries and do not alter source/audio/provenance.

## Adversarial boundary

The acceptance source has harmonic-0 constant `5`, a nonconstant pure-S `h=1` amplitude `6-4s`, a tiny active `h=2` cosine term `1/100`, and affine phase from `-1/6` to `1/2` turn. Whole-span v26 remains blocked because the `h=1` phase crosses a required half-magnitude sector boundary while global amplitude dominance is unavailable. The finite rational children before the transition are discharged by preserved phase-anchor authority; later children are discharged by preserved exact rational amplitude dominance. At least one open event root remains composition-visible.

The deterministic suite covers negative phase-rate traversal, exact `±1/1000000` phase-rate/cut neighbours, endpoint-only crossings, a phase-sector boundary coincident with an existing source spline knot, multiple cuts, coincident-cut deduplication, exact polynomial reparameterization, opposing child derivative directions, an internal-cut root counted once, multiplicity disagreement, endpoint-relation disagreement, a blocked child, exact resource refusal, forged caller cuts/children/root metadata, binary floats, source-parameter mismatch, v26 precedence, the 26-operation denominator, and false MC-B promotion. Source-knot coincidence is treated as an existing lowered-span endpoint on both sides, so it cannot create a duplicate zero-width certificate child.

## Residual blocker / next routing

PB-007-01 remains OPEN after v27. Residual cases include any exact child that remains blocked under complete v8–v26 authority, or any child whose historical certificate does not expose enough exact event/multiplicity evidence to compose safely. This is not an impossibility theorem.

The next pre-gate repair should target the remaining exact child theorem boundary rather than adding heuristic refinement. A promising direction is exact algebraic phase-cell composition using stronger sign information than fixed half-magnitude sectors, or a terminating zero-count/multiplicity authority for a broader residual trigonometric-polynomial subclass. Any such route must continue to fail closed rather than treating sampling, tolerances, numerical minimization, timeouts, or resource refusal as proof.

No native, paid, production, or expensive campaign is authorized by this result.