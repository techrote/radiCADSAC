# PB-007-01 v30 — exact source-adaptive projective rational separator

## Decision

PB-007-01 **remains OPEN**. MC-B and MC-1 remain `NOT_ESTABLISHED`; the frozen machining-domain denominator remains 26 operations. V30 adds one exact sufficient route for residual multi-harmonic spans still blocked after complete v8–v29 authority. Protected source/audio/provenance and all downstream contracts remain unchanged.

## Exact advance

V29 used one fixed projective slope, `5/12`, inside exact 1/16-turn phase cells. V30 retains those exact cells but replaces the fixed slope with a rational separator synthesized from canonical source data.

For the selected dominant/transverse source components, exact Bernstein authority derives `D_floor>0` and `T_ceiling>0`, then `beta=D_floor/T_ceiling`. Exact comparison against `tan(pi/8)=sqrt(2)-1` uses only rational arithmetic: for nonnegative rational `x`, `x>sqrt(2)-1` iff `(x+1)^2>2`.

When `beta` is strictly above the algebraic boundary, V30 defines

`f(beta)=beta^2+2 beta-1`

and

`m=beta-f(beta)/(4(beta+1))`.

The identity `beta-alpha=f(beta)/(beta+alpha+2)` for `alpha=sqrt(2)-1` proves `alpha<m<beta`. Synthesis is one finite closed-form exact calculation, not a convergent search or refinement loop.

The true mixed projection is factored projectively. The existing exact v29 phase cell supplies dominant quadrature magnitude `>12/13`, while `|tan|<=alpha<m`. Every retained derivative residual is then included in finite exact sign-orthant margins

`6|hr|(12/13)(sign(D)D-m sigma_T T)-sum sigma_i R_i > 0`

and discharged through MC-032 endpoint/Sturm strict positivity. Equality fails closed. Both selected-harmonic amplitude derivatives, harmonic-0 and every non-anchor derivative term remain residual.

## Acceptance source

The primary source has source projective ceiling `beta=83/200`. Thus `beta<5/12`, so the complete v8–v29 chain remains blocked by v29's fixed projective coefficient, but `beta>sqrt(2)-1`. The exact closed-form separator is `93867/226400`, and the complete retained residual margin is strict. The selected SIN amplitude is nonconstant (`S'=1/100000`) and a tiny h=2 term keeps a non-anchor phase residual active.

Adversarial coverage includes exact rational candidates immediately below and above the algebraic boundary (`70/169` and `169/408`), an adverse source below the boundary, exact full residual-margin equality with signed `±1/1000000` neighbours, positive/negative phase rate, symmetric SIN/COS dominance, positive/negative quadrature cells, forged caller separator/algebraic/root/multiplicity metadata, binary-float rejection, source-parameter mismatch, exact resource refusal, historical v29 precedence and blob pinning, the frozen 26-operation denominator, and false MC-B promotion rejection.

## Residual blocker

Sources outside the finite v29 phase cells remain unresolved here. So do sources for which conservative canonical Bernstein floor/ceiling data cannot place `beta` above `tan(pi/8)` even if a sharper pointwise projective argument might succeed, and sources whose complete residual orthants cannot be proved strictly positive. This is not an impossibility theorem.

The next PB-007-01 repair should attack one of those exact residuals without numerical trigonometry, sampling, epsilon, approximate algebraic comparison, adaptive subdivision, arbitrary refinement depth, timeout, or resource refusal as truth.

## Programme effect

PB-007-02 remains dependent on PB-007-01; PB-007-03 remains OPEN; PB-007-04 remains `OPEN_PROPAGATED`; PO-04/05/08 remain OPEN. Source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain protected. No native, paid, production, or expensive campaign is authorized.
