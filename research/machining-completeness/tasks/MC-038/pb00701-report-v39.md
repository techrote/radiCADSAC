# PB-007-01 v39 report — exact signed-A direct rotated-coordinate orientation

## Decision

PB-007-01 **remains open**. MC-B and MC-1 remain `NOT_ESTABLISHED`; the machining-domain denominator remains frozen at **26 operations**. V39 is a bounded exact repair layered strictly after complete v38 authority. It removes only v38's artificial `A(s)>0` orientation restriction. No native, paid, production, or expensive campaign is authorized.

## Exact construction

For source-owned exact rational selected-harmonic quadratures `C_h(s)` and `S_h(s)`, regenerate `A=(C+S)/2`, `B=(C-S)/2`, `A'=(C'+S')/2`, and `B'=(C'-S')/2`. MC-032 closed-interval endpoint/Sturm strict positivity must prove either `A>0` or `-A>0` on the complete lowered span. That proof alone derives `sigma_A in {-1,+1}`. Caller sign/orientation, rotated-coordinate, derivative, cell, margin, root, or multiplicity metadata is non-authoritative.

Define `Abar=sigma_A*A`, `Bbar=sigma_A*B`, `Abar'=sigma_A*A'`, and `Bbar'=sigma_A*B'`, so exact source authority proves `Abar>0`. On the preserved diagonal phase cell `-3/16+k/2 <= h*phi <= -1/16+k/2`, let `X=cos(theta)+sin(theta)` and `Y=cos(theta)-sin(theta)`. The proof identity is

`sigma_A*D = Abar'*X + Bbar'*Y + 2*pi*h*r*(Abar*Y-Bbar*X)`,

while the physical derivative remains exactly

`D = A'*X + B'*Y + 2*pi*h*r*(A*Y-B*X)`.

Orientation is proof bookkeeping only; no physical/source sign is discarded. V39 uses the established exact bounds `|X|<99/182`, fixed-sign `|Y|>2856/2197`, `|Y|<99/70`, and `2*pi>6`. For both `sigma_B` values, MC-032 proves `P_sigma=Abar*(2856/2197)-sigma_B*Bbar*(99/182)>0`. Every complete residual sign orthant must then prove

`6*|h*r|*P_sigma - sigma_Ap*(99/182)*Abar' - sigma_Bp*(99/70)*Bbar' - sum_i sigma_i*R_i > 0`.

Equality fails closed. Exact resource refusal remains non-truth. The original physical derivative sign is recovered as `sign(D)=sigma_A*sign(h*r)*diagonal_projection_sign`.

## Acceptance source

The primary source is the exact negation of the v38 coprime acceptance waveform: `C_1=-(1+6s/5)`, `S_1=-(11/10+6s/5)`, phase interval `[-3/16,-1/16]`, plus the live non-anchor harmonic. Complete v38 authority is executed first and is required to block this source specifically because `A=-21/20-6s/5` is strictly negative. V39 derives `sigma_A=-1`, `Abar=21/20+6s/5`, `Bbar=-1/20`, `Abar'=6/5`, and certifies the complete event without changing endpoint/root/multiplicity semantics.

Positive-`A` complete sources remain byte-for-byte owned by v38 and earlier authority. Low-level controls exercise both `sigma_A` signs. Further adversarial coverage includes exact `A=0`, rational `±1/1000000` strict-sign neighbours, sign-changing `A`, both phase-rate directions, opposite diagonal cells, exact phase-gap and complete-margin equality with rational neighbours, endpoint/root outcomes, forged `sigma_A` and oriented-polynomial evidence, source-coordinate mismatch, unsupported cells, binary floats, and exact resource refusal.

Historical v30/v37/v38 precedence remains exact and v33 blocker evidence remains pinned. No sampling, numerical trigonometry, binary-float authority, epsilon/tolerance, approximate minimization/root ordering, arbitrary subdivision/refinement depth, timeout, denominator cap, or resource budget becomes truth authority.

## Protected semantics

All protected source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain unchanged. PB-007-02 remains dependent; PB-007-03 remains open; PB-007-04 remains `OPEN_PROPAGATED`; PO-04/05/08 remain open. Do not retry MC-B from this result.
