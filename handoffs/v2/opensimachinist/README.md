# OpenSimachinist Genesis v2 handoff

This is a clean production-repository input generated from accepted radiCADSAC Genesis-v2 evidence. It is not a copy of the research repository and does not import genesis commit history.

Read `00-FOUNDING-SPEC.md` first, then the implementation roadmap, issue graph, unresolved register and bootstrap checklist. The stable programme boundary is canonical `msac-journal/1.0` manufacturing intent, immutable revisions, durable material-body/lineage identity, versioned status/error semantics and mandatory STEP engineering output. Provider and OCCT state are replaceable implementation detail.

The founding backend is semantic-provider hybrid with process-isolated OCCT 8.0.1 B-rep/STEP workers. The RCS-022 export profile remains `interoperability_unqualified` at Layer D even when file generation, self-readback, independent parsing and independent B-rep import succeed. Production code must preserve that qualification distinction.

This package preserves source/audio identity and provenance as programme-owned immutable metadata. No provider, cache rebuild, topology mapper or exporter may synthesize a replacement identity.

The clean target repository is assumed to be `techrote/OpenSimachinist`; this handoff does not create it.
