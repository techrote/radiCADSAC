# MC-027 — Multi-/tri-directional material candidate boundary

MC-027 is a **NEGATIVE_RESULT** for multi-/tri-directional interval material as the programme's total authoritative material representation. It retains directional intervals as a bounded **derived index/accelerator** after an independently certified full-3D material relation. This is candidate-selection evidence for MC-026, not a capability gate.

## Retained bounded role

A directional record may contain a finite ordered union of exact or outward-certified material intervals on one line. Multiple intervals are mandatory: a single-height-field reduction is not sufficient for cavities, reentrant sections or separated material. MC-027's exact control uses a `3 × 3 × 3` shell around a `1 × 1 × 1` central cavity; centre lines in X, Y and Z each produce exactly `[0,1] ∪ [2,3]`.

Reorientation is compatible with the derived-index role only through the explicit MC-023 common-frame transform. The exact control rotates a `2 × 1 × 1` box by a right-handed 90-degree signed-permutation transform and obtains a `1 × 2 × 1` image. Source-frame reinterpretation, inverse-transform substitution and reflections are not equivalent.

Exact boundary distinctions remain semantic. A unit box queried at `y=1`, `y=999999/1000000` and `y=1000001/1000000` distinguishes exact contact, inside and outside without a global epsilon. A positive `1/1000000` interval may not be deleted by pitch, tolerance or sewing.

## Finite-ray obstruction

Finite sampled rays are not complete material authority. In the unit cube, sample X/Y/Z rays whose transverse coordinates come from `{1/4,3/4}`. Removing the exact hidden cavity `[49/100,51/100]^3` changes material by positive volume `1/125000`, yet every sampled ray remains `[0,1]` because all of them miss the cavity.

The argument is general: a finite family of lines in finitely many directions has empty 3D interior, so a sufficiently small positive-volume rational box can be placed away from their union. Denser finite sampling changes the blind spot but does not remove the information-loss result.

## Continuous-field boundary

Defining intervals for every line can characterize material mathematically, but a finite exact/certified implementation must encode the transverse events at which interval count, endpoint order, split/merge state or boundary relation changes. Those event sets and unresolved queries require an independently justified full-3D material relation.

Therefore the directional structure is useful after certification, but it does not supply certification merely by existing. Three directions do not vote on truth; union/intersection/majority heuristics are forbidden. Missing or unresolved rays remain `UNCERTIFIED` rather than becoming material or empty by convention.

This is especially important for MC-019: genuine simultaneous-XYZ rounded sweeps and complete finite cutter extent cannot be replaced by independent directional projections. MC-023 likewise keeps all directional queries in the durable common workpiece frame.

## Identity/output boundary

Directional interval labels are not durable body identity or lineage, and directional connectivity is not a substitute for journal `material_body_transition` authority. Existing output blockers remain open rather than being duplicated:

- `RB-016-02` — durable multi-body engineering-output mapping and independent-consumer preservation remain unqualified;
- `RB-016-04` — exact-zero contacts, touching cavities and singular pinches remain subject to the approved profile and independent-consumer evidence without healing or tolerance fusion.

No native or paid campaign ran. MC-A remains `ACCEPTED`; **MC-B, MC-C, MC-D, MC-E, MC-F and MC-1 remain `NOT_ESTABLISHED`**. Protected historical source/audio/provenance, canonical-journal, positive-volume and durable body/lineage semantics are unchanged.
