# PB-007-01 v30 — exact source-adaptive projective rational separator

**RAG: GREEN for this bounded extension; RED for the full PB-007-01 obligation.**

PB-007-01 remains OPEN. MC-B and MC-1 remain `NOT_ESTABLISHED`. The machining-domain denominator remains frozen at 26 operations. This document records an exact sufficient residual route only; it does not narrow the domain or weaken protected source/audio/provenance or downstream contracts.

## Construction

Complete v8–v29 authority retains strict precedence. V30 is considered only for an exact lowered multi-harmonic span that still fails closed inside a v29 1/16-turn algebraic phase cell because v29's fixed projective slope `5/12` is too conservative.

For an S-dominant COS cell,

`S*cos - C*sin = cos*(S-C*tan)`.

The exact endpoint projective boundary is `tan(pi/8)=sqrt(2)-1`. The C-dominant SIN-cell route is symmetric. V30 reuses v29's exact rational-turn cell geometry and exact dominant quadrature bound `|cos|>12/13` or `|sin|>12/13`; it does not add another angle table.

From canonical source polynomials, exact Bernstein authority derives a strict dominant floor `D_floor` and transverse absolute ceiling `T_ceiling`. Define the source-owned rational

`beta = D_floor/T_ceiling`.

If exact rational arithmetic cannot prove `beta > sqrt(2)-1`, V30 fails closed. For nonnegative rational `x`, the comparison is exact because

`x > sqrt(2)-1  <=>  (x+1)^2 > 2`.

When `beta` is above the algebraic boundary, let `f(beta)=beta^2+2 beta-1` and synthesize

`m = beta - f(beta)/(4(beta+1))`.

Writing `alpha=sqrt(2)-1`, the exact identity

`beta-alpha = f(beta)/(beta+alpha+2)`

and `alpha<beta` imply

`beta-alpha > f(beta)/(2(beta+1))`.

Therefore `alpha < m < beta`. This is a closed-form rational construction from source-owned data. No continued-fraction loop, unbounded search, arbitrary refinement depth, timeout, sampling, numerical trigonometry, epsilon, or approximate algebraic comparison participates.

## Residual dominance

Both selected-harmonic amplitude derivatives remain explicit residuals. Harmonic-0 and every non-anchor derivative contribution remain explicit residuals as well.

For each finite sign orthant V30 proves the exact polynomial margin

`6|hr|(12/13)(sign(D)D(s) - m sigma_T T(s)) - sum sigma_i R_i(s) > 0`

on the complete closed source span using MC-032 exact rational endpoint/Sturm strict-positivity authority. Because `2*pi>6`, `|dominant quadrature|>12/13`, and `|transverse/dominant quadrature|<=tan(pi/8)<m`, these strict margins prove a fixed nonzero complete derivative. Equality fails closed.

Established exact rational-turn endpoint authority then decides zero/open/endpoint root relations. Every admitted root is simple because the derivative is bounded away from zero.

## Adversarial boundary

The acceptance source is genuinely beyond complete v8–v29 authority: its source projective ceiling is `beta=83/200`, so `beta<5/12` defeats v29's fixed slope while `beta>sqrt(2)-1` permits the exact source-adaptive separator `m=93867/226400`.

The tests also cover a source projective ceiling below the algebraic boundary, exact rational comparison controls `70/169 < sqrt(2)-1 < 169/408`, exact full residual-margin equality with signed `±1/1000000` neighbours, positive and negative phase rate, SIN- and COS-dominant orientations, positive and negative quadrature cells, active selected-harmonic amplitude derivative, active non-anchor phase residual, forged caller metadata, binary-float rejection, source-parameter mismatch, exact resource refusal, and historical v29 precedence.

No rational candidate can equal the irrational boundary `sqrt(2)-1`; the exact boundary control therefore uses rational candidates immediately on both sides rather than pretending an exact rational equality exists.

## Preserved contracts

V27 certificate-cut composition, endpoint/root/multiplicity semantics, the 26-operation domain, protected source/audio/provenance, canonical journal, exact time/path/phase, source uncertainty, positive-volume material, cutter/holder access, durable body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain unchanged.

Residual cases beyond v30 remain PB-007-01 OPEN. This result is not an impossibility theorem and does not establish MC-B or MC-1. No native, paid, production, or expensive campaign is authorized.
