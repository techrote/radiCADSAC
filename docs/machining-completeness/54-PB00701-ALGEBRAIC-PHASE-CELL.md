# PB-007-01 v28 — exact algebraic rational-turn phase-cell projection

**RAG: GREEN for this bounded extension; RED for the full PB-007-01 obligation.**

PB-007-01 remains OPEN. MC-B and MC-1 remain `NOT_ESTABLISHED`. The machining-domain denominator remains frozen at 26 operations. This document records an exact sufficient residual route only; it does not narrow the domain or weaken source/audio/provenance or downstream contracts.

## Green construction

Complete v8–v27 authority retains strict precedence. V28 is considered only for an exact lowered multi-harmonic span that still fails closed because the historical half-magnitude component-cone bound is too conservative.

For an S-dominant mixed phase projection, v28 accepts an exact harmonic phase interval contained in a rational-turn COS cell `[-1/12,1/12]+k` or `[5/12,7/12]+k`. On those cells, `|cos| >= sqrt(3)/2 > 6/7` while `|sin| <= 1/2`, with fixed COS sign. The comparison is exact: `sqrt(3)/2 > 6/7` follows by squaring positive quantities, `3/4 > 36/49`, equivalently `147 > 144`. The C-dominant route is symmetric on SIN cells `[1/6,1/3]+k` and `[2/3,5/6]+k`. Cell containment and all boundaries are exact rational-turn comparisons; no numerical trigonometry is used.

For a fixed-sign dominant source component `D(s)` and transverse component `T(s)`, every retained residual polynomial `R_i(s)` is discharged simultaneously by the finite family

`6|hr|((6/7) sign(D) D(s) - (1/2) sigma_T T(s)) - sum sigma_i R_i(s) > 0`

for all sign choices. MC-032 exact rational endpoint/Sturm strict-positivity authority proves each margin. Since `2*pi > 6`, this proves the true selected phase projection dominates the complete retained residual. Both selected-harmonic amplitude derivatives remain residual; harmonic-0 and all non-anchor harmonic derivative terms remain residual as well.

Established exact rational-turn endpoint authority then decides endpoint relations. A certified complete derivative has fixed nonzero sign, so any admitted root is simple and exact zero/open/endpoint event semantics remain unchanged.

## Boundary and composition

Exact equality fails closed. Phase intervals outside a supported cell are not approximated, sampled, or adaptively subdivided by v28. Caller-supplied cells, algebraic values, projection/separation bounds, root counts, multiplicities, and certificates are ignored.

V27 certificate-cut composition remains authoritative and unchanged. Its exact external-endpoint handling, source-knot coincidence behavior, internal-cut root deduplication, explicit compatible multiplicity requirement, and support for adjacent children with opposing derivative directions are all revalidated by the v28 verifier. A phase-cell boundary is certificate geometry, never a new physical event.

## Red residual boundary

Residual cases outside these finite exact algebraic cells, or cases whose polynomial projection/residual margins cannot be proved strictly positive, remain blocked. This is not an impossibility theorem. New transcendental minimization/zero authority is not inferred from this result.

No binary float, epsilon/tolerance, sampling, numerical trigonometry, approximate algebraic comparison, approximate root ordering, adaptive subdivision, arbitrary refinement depth, timeout, or resource refusal participates in correctness. Resource refusal remains non-truth.

## Preserved contracts

Protected source/audio/provenance, canonical-journal semantics, exact time/path/phase, source uncertainty, positive-volume material, cutter/holder access, durable body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain unchanged. Historical v27 evidence is pinned by exact git blob SHA in the v28 boundary artifact.

No native, paid, production, or expensive campaign is authorized.
