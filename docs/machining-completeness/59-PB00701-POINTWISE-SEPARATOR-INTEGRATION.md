# PB-007-01 v33 — exact pointwise separator integration

## Status

**AMBER / blocker preserved.** The v32 exact algebraic separator foundation is now integrated with the complete v30 residual sign-orthant proof as a deterministic diagnostic path. No new event-classifier family is promoted because the targeted residual sources remain blocked by the unchanged selected-harmonic amplitude-derivative envelope. PB-007-01 remains OPEN; MC-B and MC-1 remain `NOT_ESTABLISHED`.

## What v33 establishes

- V32 can now supply a canonical, source-bound rational separator directly to the preserved v30 projective/residual verifier.
- The separator is independently checked against its canonical source binding before use.
- Complete historical v8-v30 executable authority retains precedence.
- Both selected-harmonic amplitude derivatives, harmonic-0 and every non-anchor derivative term remain explicit residuals.
- The integration remains finite: exact v32 Sturm/algebraic critical-value ordering and closed-form rational separator synthesis followed by finite v30 sign orthants.
- Exact resource refusal remains non-truth; no sampling, numerical trigonometry, binary float, epsilon, arbitrary denominator/depth cap, or timeout becomes correctness authority.

## Exact boundary exposed

For the v31 diagnostic `D=1+2s`, `T=2+3s`, the historical global Bernstein ratio `1/5` blocks v30 while v32 now synthesizes a source-owned separator strictly above `sqrt(2)-1`. Nevertheless, the preserved full-residual orthants still fail because the selected amplitude derivatives remain `2` and `3`.

The common-factor control `C=S=(1+3s/4)^2` sharpens that distinction. Its pointwise projective ratio is exactly one but the separate global Bernstein ratio is `16/49 < sqrt(2)-1`. V32 cancels the exact common factor and certifies separator synthesis; the preserved independent amplitude-derivative envelope still blocks complete derivative authority.

This is not an impossibility theorem. It is a precise repair boundary: separator synthesis is no longer the missing primitive; the next conservatism is the loss of phase correlation when `C' cos + S' sin` is replaced by independent absolute-value residual terms.

## Next dependency-ready route

PB-007-01 v34 should introduce an exact phase-correlated selected-harmonic amplitude/phase derivative certificate for a bounded source-owned proportional/common-factor quadrature family. A useful first construction is an exact diagonal rational-turn cell where a rotated quadrature gives a strong lower bound for the selected phase derivative and a strict upper bound for the joint amplitude derivative. Non-anchor residuals and endpoint/root/multiplicity authority remain unchanged.

The machining-domain denominator remains 26 operations. Source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics are preserved.
