# PB-007-01 v36 — phase-correlated varying-ratio shared-factor authority

PB-007-01 **remains open**. MC-B and MC-1 remain `NOT_ESTABLISHED`; the machining-domain denominator remains frozen at **26 operations**.

V36 is a bounded exact extension of v35. For one selected positive harmonic, source-owned coefficients must regenerate exactly as

`C_h(s)=lambda(s)G(s)`, `S_h(s)=G(s)`

with a nonconstant exact rational polynomial `lambda(s)` and an exact rational polynomial `G(s)` of one strict sign. `lambda` is derived by exact rational-polynomial division of `C_h` by `S_h`, with zero remainder required; forged ratio, division, common-factor, cell, bound, margin, root, or multiplicity metadata is non-authoritative.

On the preserved diagonal cell `-3/16+k/2 <= h*phi <= -1/16+k/2`, use the existing exact bounds `L=2856/2197`, `U=99/182`, `W=99/70`, `|cos(theta)|<=1`, and `2*pi>6`. Set `A=(lambda+1)/2`, `B=(lambda-1)/2`, and retain the exact selected derivative

`G'*(A*X+B*Y)+lambda'*G*cos(theta)+2*pi*h*r*G*(A*Y-B*X)`.

The `lambda'*G*cos(theta)` term is mandatory evidence. MC-032 exact endpoint/Sturm authority proves `A(s)>0`, both phase-gap polynomials `A*L-sigma_B*B*U>0` for `sigma_B in {-1,+1}`, and every complete sign-orthant margin over `A*G'`, `B*G'`, `lambda'*G`, and all non-anchor residual polynomials. Equality and resource refusal fail closed.

The acceptance source uses `lambda=9/10+s/100`, `G=(1+3s/4)^2`, phase interval `[-3/16,-1/16]`, and a live non-anchor harmonic. V35 blocks because proportionality is nonconstant; v36 retains `lambda'=1/100` and certifies the complete event. Adversarial controls cover signed `1/1000000` neighbours at the phase cell, `A>0`, pointwise phase-gap, and complete-margin boundaries; zero/open/left-endpoint/right-endpoint outcomes; nonzero division remainder; zero/sign-changing `G`; source-coordinate mismatch; binary floats; forged authority; and exact resource refusal.

Historical v30, v34, and v35 successes retain precedence, while v33 blocker evidence remains pinned. All protected source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain unchanged. No native, paid, production, or expensive campaign is authorized.

The next bounded repair frontier is an exact two-polynomial shared-factor family `C_h=N(s)G(s)`, `S_h=D(s)G(s)` with both `N'G` and `D'G` retained and no assumption that `C/S` is polynomial. If existing finite exact rational-polynomial/Sturm authority cannot establish it, the theorem boundary must be recorded rather than approximated.
