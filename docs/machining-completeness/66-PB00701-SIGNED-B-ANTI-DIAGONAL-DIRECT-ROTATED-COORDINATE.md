# PB-007-01 v40: signed-B anti-diagonal direct rotated-coordinate authority

V40 is a bounded exact extension of the PB-007-01 analytic event classifier. It does **not** establish PB-007-01 globally and does not establish MC-B or MC-1.

## Authority boundary

Complete v39 authority retains strict precedence. V40 may act only on an exact lowered span that remains blocked after v39 and has two nonzero source-owned rational-polynomial quadratures for a selected positive harmonic.

From the source regenerate `A=(C+S)/2`, `B=(C-S)/2` and exact derivatives. MC-032 closed-interval endpoint/Sturm authority must prove one strict sign of `B`; V40 derives `sigma_B` and never trusts caller orientation metadata. Define `Bbar=sigma_B B>0`, `Atilde=-sigma_B A`, `Bbar'=sigma_B B'`, and `Atilde'=-sigma_B A'`.

The admitted phase cell is exactly

`1/16+k/2 <= h*phi <= 3/16+k/2`.

This anti-diagonal cell is qualified by exact quarter-turn symmetry with the existing diagonal certificate, not by numerical trigonometry. The transferred exact bounds are `|X|>2856/2197`, `|Y|<99/182`, `|X|<99/70`, with fixed sign of `X` and `2*pi>6`, for `X=cos+sin`, `Y=cos-sin`.

For each `sigma_A` in `{-1,+1}`, MC-032 must prove the source-owned phase gap

`Bbar*(2856/2197)-sigma_A*Atilde*(99/182)>0`.

For both such gaps and every finite sign orthant over `Atilde'`, `Bbar'`, and every non-anchor residual, MC-032 must prove the complete strict margin

`6|hr|P - sigma_Ap*(99/70)Atilde' - sigma_Bp*(99/182)Bbar' - sum sigma_i R_i > 0`.

Equality is not certification. Exact resource refusal is not a truth value.

## Exact oriented identity and #243 reconciliation

The exact physical selected-harmonic derivative remains

`D=A'X+B'Y+2*pi*h*r*(AY-BX)`.

With the v40 definitions, multiplying by `-sigma_B` gives

`(-sigma_B)D=Atilde'X-Bbar'Y+2*pi*h*r*(Bbar X+Atilde Y)`.

Issue #243 wrote a plus sign before `Bbar'Y`; that is an algebraic typo. The executable and verifier use the exact minus sign. Since both signs of the `Bbar'` channel are enumerated in the finite orthant proof, the complete strict-margin contract is unchanged. Certified physical sign recovery is

`sign(D)=-sigma_B*sign(h*r)*anti_diagonal_projection_sign`.

## Fail-closed requirements

V40 rejects or propagates refusal for a zero/sign-changing `B`, unsupported phase cell, failed phase gap or complete margin, binary-float source authority, source-coordinate mismatch, malformed exact source structure, or any attempt to substitute caller sign/orientation/cell/bound/margin/root metadata. Sampling, epsilon/tolerance, numerical trigonometry, approximate root ordering, arbitrary refinement/subdivision depth, timeout, denominator cap and resource budget are not correctness authority.

Endpoint/root/multiplicity composition is inherited unchanged from the qualified exact predecessor route. A certificate-cell boundary is proof geometry, not a new physical event.

## Programme preservation

The machining-domain denominator remains 26 operations. PB-007-01 remains OPEN; PB-007-02 remains dependent; PB-007-03 remains OPEN; PB-007-04 remains `OPEN_PROPAGATED`; PO-04, PO-05 and PO-08 remain OPEN; MC-B and MC-1 remain `NOT_ESTABLISHED`. Do not retry MC-B solely because v40 lands.

All source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics are protected unchanged. No native, paid, production or expensive campaign is authorized.
