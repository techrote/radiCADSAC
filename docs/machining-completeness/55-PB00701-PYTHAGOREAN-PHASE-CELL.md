# PB-007-01 v29 — exact 5-12-13 nested algebraic phase-cell projection

**RAG: GREEN for this bounded extension; RED for the full PB-007-01 obligation.**

PB-007-01 remains OPEN. MC-B and MC-1 remain `NOT_ESTABLISHED`. The machining-domain denominator remains frozen at 26 operations. This document records an exact sufficient residual route only; it does not narrow the domain or weaken source/audio/provenance or downstream contracts.

## Green construction

Complete v8–v28 authority retains strict precedence. V29 is considered only for an exact lowered multi-harmonic span that still fails closed because v28's rational `6/7` dominant and `1/2` transverse projection bounds are too conservative.

For an S-dominant mixed phase projection, v29 accepts an exact harmonic phase interval contained in a rational-turn COS cell `[-1/16,1/16]+k` or `[7/16,9/16]+k`. For the symmetric C-dominant route it accepts SIN cells `[3/16,5/16]+k` or `[11/16,13/16]+k`. At every supported cell endpoint the dominant magnitude is `cos(pi/8)=sqrt(2+sqrt(2))/2` and the transverse magnitude is `sin(pi/8)=sqrt(2-sqrt(2))/2`.

The irrational endpoint comparison is eliminated exactly. Since `sqrt(2)>238/169`, proved by the positive rational square comparison `57122>56644`, we obtain

`cos(pi/8)>12/13` and `sin(pi/8)<5/13`.

For a fixed-sign dominant source component `D(s)`, transverse component `T(s)`, and every retained residual polynomial `R_i(s)`, v29 requires every exact finite sign orthant

`6|hr|((12/13) sign(D)D(s) - (5/13) sigma_T T(s)) - sum sigma_i R_i(s) > 0`.

MC-032 exact rational endpoint/Sturm strict-positivity authority proves each margin. Because `2*pi>6`, success proves that the true selected phase projection dominates the complete retained residual pointwise. Both selected-harmonic amplitude derivatives remain residual; harmonic-0 and all non-anchor derivative terms remain residual as well.

Established exact rational-turn endpoint authority then decides endpoint relations. A certified complete derivative has fixed nonzero sign, so every admitted root remains simple and the existing zero/open/endpoint event grammar is unchanged.

## Boundary and composition

Exact equality fails closed. Phase intervals outside a supported 1/16-turn cell are not sampled, approximated, or adaptively subdivided by v29. Caller-supplied phase cells, algebraic values, projection/separation bounds, root counts, multiplicities, and certificates are ignored.

V27 certificate-cut composition remains authoritative through complete v28 precedence. Its exact external-endpoint handling, source-knot coincidence behavior, internal-cut root deduplication, explicit multiplicity compatibility, and support for adjacent children with opposing derivative directions remain unchanged. A phase-cell boundary is certificate geometry, never a new physical event.

## Red residual boundary

Residual cases outside these finite exact nested cells, or cases whose projective/residual margins cannot be proved strictly positive, remain blocked. This is not an impossibility theorem. New transcendental minimization/zero authority is not inferred from this result.

No binary float, epsilon/tolerance, sampling, numerical trigonometry, approximate algebraic comparison, approximate root ordering, adaptive subdivision, arbitrary refinement depth, timeout, or resource refusal participates in correctness. Resource refusal remains non-truth.

## Preserved contracts

Protected source/audio/provenance, canonical-journal semantics, exact time/path/phase, source uncertainty, positive-volume material, cutter/holder access, durable body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain unchanged. Historical v28 evidence is pinned by exact git blob SHA in the v29 boundary artifact.

No native, paid, production, or expensive campaign is authorized.
