# PB-007-01 v41 — v42-aware orientation-root partition composition

**RAG: GREEN only if the bounded exact v42-residual composition acceptance and adversarial contract pass; RED for full PB-007-01.**

PB-007-01 remains OPEN. MC-B and MC-1 remain `NOT_ESTABLISHED`; the machining denominator remains frozen at 26 operations.

## Authority

Verified v42 / #247 / PR #248 at `ab9bc9e52a7db184f10bb6d4b7101cf0682a48d2` is the required predecessor. The failed #246 v40-only composition is retained as negative evidence and is not revived.

Complete current predecessor authority through v42 runs first. Only residual exact lowered spans are eligible for new partition/composition authority.

## Construction

V41 regenerates source-owned `A=(C+S)/2` and `B=(C-S)/2`. Interior rational orientation roots are accepted only when exact Sturm root counting proves the rational enumeration complete. Endpoint roots are recorded without creating zero-width children. Cuts are unioned with historical v27 phase-sector cuts and deduplicated exactly.

Each child is exactly reparameterized and rerun through the complete v42 classifier. This preserves all earlier route precedence and permits the v42 transition bridge only when its source-derived strict-`B'` and exact margin premises actually hold. A cut at `A=0` or `B=0` is proof geometry only; it does not make a zero-adjacent closed child v39/v40-admissible.

Child events are composed by the historical v27 exact endpoint/root/multiplicity composer. Proof cuts are event-neutral absent matching physical equality; matching equality with matching multiplicity is counted once; disagreement fails closed.

## Acceptance and refusal boundary

A new acceptance source must remain blocked after complete v42 authority and become certified only after orientation-root partition plus independent child/handoff authority. At least one child must use the v42 transition bridge and the composed children must exhibit more than one predecessor ownership route. The exact reverse phase law is also exercised.

Irrational algebraic orientation roots unsupported by the rational child map, exact resource refusal, unresolved child authority, source/provenance mismatch, binary floats, approximate roots, epsilon/tolerance, sampling, adaptive refinement, arbitrary subdivision, timeout, or denominator caps remain non-truth.

Historical v27 and v30/v34/v35/v36/v37/v38/v39/v40/v42 authority retains precedence. Protected semantics remain unchanged. MC-B is not retried.
