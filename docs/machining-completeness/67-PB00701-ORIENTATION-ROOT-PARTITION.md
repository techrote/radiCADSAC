# PB-007-01 v41 — exact orientation-root partition composition

**RAG: GREEN only for the bounded exact rational-root partition extension; RED for full PB-007-01.**

PB-007-01 remains OPEN. MC-B and MC-1 remain `NOT_ESTABLISHED`; the machining denominator remains frozen at 26 operations.

## Construction

Complete v40 authority runs first. For a residual exact lowered span, v41 regenerates `A=(C+S)/2` and `B=(C-S)/2` from source-owned rational quadrature polynomials. Interior rational orientation roots are derived by the rational-root theorem and are accepted only when an independent exact Sturm distinct-root count proves that those rational roots exhaust every interior root. Multiplicity is obtained by exact polynomial division. Endpoint roots are recorded but do not create interior children.

The orientation cuts are unioned with preserved v27 rational phase-sector cuts, then sorted and deduplicated exactly. Each child is reparameterized with the existing v27 exact polynomial/phase map and rerun through complete v40 authority. Child event evidence is composed with the historical v27 exact endpoint/root/multiplicity composer. A proof cut is event-neutral unless both adjacent exact endpoint relations establish the same physical equality with matching multiplicity.

## Exact refusal boundary

The current child representation is rational. If Sturm proves an interior orientation root exists but the rational-root theorem cannot represent it exactly, v41 returns the explicit blocker `IRRATIONAL_ALGEBRAIC_ORIENTATION_ROOT_NOT_REPRESENTABLE_BY_CURRENT_RATIONAL_CHILD_PARAMETER_MODEL`; it does not insert an approximate cut. Resource refusal, unresolved child authority, endpoint/multiplicity disagreement, or source/provenance mismatch remains non-truth/fail-closed.

No binary-float authority, epsilon/tolerance, numerical trigonometry, sampling, approximate root ordering, adaptive refinement, arbitrary subdivision depth, timeout or denominator cap is correctness authority.

## Preserved contracts

Historical v27 and v30/v34/v35/v36/v37/v38/v39/v40 authority retains precedence. Protected source/audio/provenance, canonical journal, exact time/path/phase, source uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics are unchanged. MC-B is not retried.
