# PB-007-01 v37 — phase-correlated two-polynomial shared-factor authority

PB-007-01 **remains open**. MC-B and MC-1 remain `NOT_ESTABLISHED`; the machining-domain denominator remains frozen at **26 operations**.

V37 is a bounded exact extension of v36. For one selected positive harmonic, source-owned exact rational polynomials must regenerate as

`C_h(s)=N(s)G(s)`, `S_h(s)=D(s)G(s)`

where `G` is the canonical **monic Euclidean GCD over Q[s]**, is nonconstant, and has one strict sign on the complete closed lowered span. Both divisions must have exact zero remainder and both source products are regenerated coefficientwise. Caller GCD, quotient, factor, sign, cell, bound, margin, root, and multiplicity metadata remain non-authoritative. Coprime quadratures and constant GCDs are intentionally outside this bounded route.

Set `A=(N+D)/2`, `B=(N-D)/2`, `X=cos(theta)+sin(theta)`, and `Y=cos(theta)-sin(theta)`. On the preserved diagonal phase cell `-3/16+k/2 <= h*phi <= -1/16+k/2`, v37 uses only the established exact rational bounds `|X|<99/182`, fixed-sign `|Y|>2856/2197`, `|Y|<99/70`, and `2*pi>6`. The complete selected-harmonic derivative is retained as

`G'*(A*X+B*Y)+G*(A'*X+B'*Y)+2*pi*h*r*G*(A*Y-B*X)`.

Equivalently, the new quotient-derivative term is the complete `N'*G*cos(theta)+D'*G*sin(theta)` contribution. Both quotient derivatives remain material evidence. MC-032 exact endpoint/Sturm authority proves `A(s)>0`, both phase-gap polynomials `A*2856/2197-sigma_B*B*99/182>0`, and every complete sign-orthant margin over `A*G'`, `B*G'`, `A'*G`, `B'*G`, and all non-anchor residual polynomials. Equality and exact resource refusal fail closed.

The acceptance source uses source factor `(1+3s/4)^2`, `N_source=9/10+s/100`, `D_source=1+s/200`, phase interval `[-3/16,-1/16]`, and a live non-anchor harmonic. V36 blocks because `C/S=N_source/D_source` is not an exact polynomial. V37 derives canonical monic `G=16/9+8s/3+s^2`, `N=81/160+9s/1600`, and `D=9/16+9s/3200`; both quotient derivatives are nonzero and the complete event is certified.

Adversarial controls cover both phase-rate directions and opposite diagonal cells; exact cell, `A>0`, phase-gap, and complete-margin equality boundaries with signed rational `1/1000000` neighbours; zero/open/left-endpoint/right-endpoint outcomes; coprime quadratures; zero/constant/sign-changing factors; malformed GCD division; source-coordinate mismatch; binary floats; forged authority; and exact resource refusal. Historical v30, v34, v35, and v36 successes keep exact precedence and v33 blocker evidence remains pinned.

All protected source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain unchanged. No native, paid, production, or expensive campaign is authorized.

The next bounded repair frontier is to remove the nonconstant-common-factor requirement entirely: operate directly on source-owned quadratures with `A=(C+S)/2`, `B=(C-S)/2`, retain `A'X+B'Y` and the correlated phase term, and discharge the same finite exact phase-gap/residual sign family. If that direct construction cannot be established with current MC-032 authority, the precise theorem boundary must be recorded instead of introducing numerical trigonometry, tolerances, sampling, or arbitrary refinement.
