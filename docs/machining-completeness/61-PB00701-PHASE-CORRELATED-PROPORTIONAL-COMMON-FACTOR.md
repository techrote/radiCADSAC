# PB-007-01 v35 — phase-correlated proportional common-factor contract

## Contract status

This document records a bounded exact corrective extension for MC-038/PB-007-01. **PB-007-01 remains open** after this extension. MC-B and MC-1 remain `NOT_ESTABLISHED`. The machining-domain denominator remains frozen at **26 operations**.

## Source-owned family

The selected positive harmonic must regenerate exactly from source coefficients as

`C_h(s)=lambda*G(s)`, `S_h(s)=G(s)`,

where both quadratures are nonzero, `lambda` is a nonzero exact rational constant, and `G` is an exact rational polynomial with one strict sign on the complete lowered closed source span. `lambda` is derived from the first nonzero source coefficient of `S_h`; every coefficient identity `C=lambda*G` is then checked exactly. Caller lambda, common-factor, sign, cell, bound, margin, root, multiplicity, or Sturm metadata is non-authoritative.

Complete v8-v30 executable authority runs first and v34 retains exact precedence for `C_h=S_h=G`. V33 blocker evidence remains evidence and is not rewritten as success.

## Phase-correlated derivative authority

On an exact rational diagonal cell

`-3/16+k/2 <= h*phi <= -1/16+k/2`,

let `X=cos(theta)+sin(theta)` and `Y=cos(theta)-sin(theta)`. Existing exact algebraic authority supplies

- `|X| < 99/182`;
- `Y` has fixed sign and `|Y| > 2856/2197`;
- `|Y| < 99/70`;
- `2*pi > 6`.

For `A=(lambda+1)/2`, `B=(lambda-1)/2`, the selected harmonic derivative is kept correlated as

`G'*(A*X+B*Y) + 2*pi*h*r*G*(A*Y-B*X)`.

V35 requires `A>0` and the exact strict phase gap

`P=A*(2856/2197)-|B|*(99/182)>0`.

The selected phase part then has a fixed exact sign and magnitude exceeding `6*|h*r|*P*|G|`. The selected amplitude part is bounded by `Q*|G'|`, where

`Q=|A|*(99/182)+|B|*(99/70)`.

The complete derivative is certified only when finite exact rational-polynomial sign orthants plus MC-032 closed-interval endpoint/Sturm authority prove

`6*|h*r|*P*|G(s)|-Q*|G'(s)|-sum_i|R_i(s)|>0`

on the complete closed span. Every non-anchor derivative contribution stays explicit in `R_i`. Both selected amplitude derivative channels and both selected phase channels are consumed only through the correlated identity. Equality fails closed.

## Boundary and refusal semantics

The exact phase-gap boundary for this orientation is `lambda=-7751/18905`, where `P=0`; equality is not authority. Signed rational `1/1000000` neighbours are adversarial controls, not tolerances. The same rule applies to phase-cell and complete-margin equalities.

Malformed or nonconstant proportionality, source coefficient perturbation, zero/sign-changing `G`, `A<=0`, unsupported cells, source-coordinate mismatch, forged proof metadata, and binary floats fail closed. Exact resource refusal remains non-truth and must not be promoted to a mathematical result.

Zero/open/left-endpoint/right-endpoint event outcomes retain the existing exact endpoint/root/multiplicity authority. Historical v30 and v34 successes retain exact precedence. False MC-B promotion remains rejected.

## Protected semantics

This corrective route does not alter source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, or conventional STEP semantics. No native, paid, production, or expensive campaign is authorized by this result.

## Remaining frontier

PB-007-01 remains open beyond constant proportional common factors. The next useful repair is a finite exact source-owned varying-ratio/shared-factor construction that keeps selected amplitude and phase derivatives correlated and retains every non-anchor residual. If current exact algebraic/Sturm authority cannot prove such a family, record the precise theorem blocker rather than introducing sampling, numerical trigonometry, tolerance, approximate minimization/root ordering, arbitrary refinement depth, timeout, denominator cap, or resource budget as truth authority.
