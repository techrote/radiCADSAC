# PB-007-01 v39 — signed-A direct rotated-coordinate orientation authority

PB-007-01 **remains open**. MC-B and MC-1 remain `NOT_ESTABLISHED`; the machining-domain denominator remains frozen at **26 operations**.

V39 extends v38 only after complete v38 authority blocks. It removes the artificial positive-orientation requirement `A(s)>0` by deriving a strict source-owned sign for `A=(C+S)/2` with MC-032 exact endpoint/Sturm authority. If `A>0`, complete sources remain owned by v38. If `-A>0`, v39 derives `sigma_A=-1`; if `A` is zero anywhere, changes sign, or exact sign determination refuses resources, v39 fails closed.

The oriented coordinates are `Abar=sigma_A*A`, `Bbar=sigma_A*B`, `Abar'=sigma_A*A'`, and `Bbar'=sigma_A*B'`. On `-3/16+k/2 <= h*phi <= -1/16+k/2`, with `X=cos(theta)+sin(theta)` and `Y=cos(theta)-sin(theta)`, v39 proves the exact identity

`sigma_A*D=Abar'*X+Bbar'*Y+2*pi*h*r*(Abar*Y-Bbar*X)`.

The physical derivative is unchanged: `D=A'*X+B'*Y+2*pi*h*r*(A*Y-B*X)`. Hence `sign(D)=sigma_A*sign(h*r)*diagonal_projection_sign`. Orientation is proof bookkeeping, not a source or physical transformation.

The established exact rational bounds remain `|X|<99/182`, fixed-sign `|Y|>2856/2197`, `|Y|<99/70`, and `2*pi>6`. Both oriented phase-gap polynomials and every complete sign-orthant margin over `Abar'`, `Bbar'`, and all retained non-anchor residuals are proved strictly positive with MC-032. Equality and exact resource refusal fail closed.

The acceptance source is `C_1=-(1+6s/5)`, `S_1=-(11/10+6s/5)` with phase interval `[-3/16,-1/16]` and a live non-anchor harmonic. Complete v38 blocks it specifically at its strict-negative `A`; v39 derives `sigma_A=-1` and certifies it. Adversarial controls cover both orientation signs, `A=0`, sign change, rational `1/1000000` neighbours, both rate directions, opposite cells, phase-gap and complete-margin equality, endpoint/root/multiplicity preservation, forged orientation/coordinate/derivative authority, binary floats, source-coordinate mismatch, unsupported cells, and resource refusal.

Historical v30/v37/v38 precedence and v33 blocker evidence remain pinned. All protected source/audio/provenance, canonical-journal, exact time/path/phase, source-uncertainty, positive-volume material, cutter/holder, durable-body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain unchanged. No native, paid, production, or expensive campaign is authorized; do not retry MC-B.
