# PB-007-01 v32 — exact algebraic separator foundation

**RAG: GREEN for the v31 missing algebraic prerequisite; RED for the full PB-007-01 obligation.**

PB-007-01 remains OPEN. MC-B and MC-1 remain `NOT_ESTABLISHED`. The machining-domain denominator remains frozen at **26 operations**, and all protected source/audio/provenance and downstream semantics remain unchanged.

## Qualified prerequisite

V32 supplies the source-owned finite exact operation missing from the v31 pointwise projective route. For exact rational `D(s)` and `T(s)` with the dominant sign independently certified, it constructs the stationary polynomial `D'T-DT'`, isolates transverse and stationary roots with MC-032 Sturm authority, retains repeated-root multiplicity, and forms an exact critical-value resultant for `D^2-y^2T^2` plus closed-span endpoint factors.

Real algebraic critical values are ordered against `tan(pi/8)=sqrt(2)-1` without binary floating point. Exact sign-at-algebraic-root decisions use polynomial gcd plus Sturm root exclusion; root isolation/order termination is bounded from canonical source coefficients by Cauchy root bounds and a conservative Mignotte-derived rational root-separation bound.

If the complete-span minimum is strictly above `sqrt(2)-1`, a rational lower enclosure `beta` is converted to a source-derived separator with the closed form

`m = beta - (beta^2+2beta-1)/(4(beta+1))`.

The final authority is independently checked by strict positivity of both source polynomials `sign(D)D-m*T` and `sign(D)D+m*T` on `[0,1]` under MC-032 endpoint/Sturm authority. Exact equality, unresolved exact algebra, semantic mismatch and resource refusal fail closed.

## Boundaries

The v31 `D=1+2s`, `T=2+3s` diagnostic is now constructively solvable without trusting its old supplied `m=5/12`. Adversarial coverage includes irrational interior stationary minima, endpoint minima, transverse sign-cell partitioning, coincident transverse/stationary zeros, repeated stationary roots, exact `sqrt(2)-1` equality and signed `±1/1000000` neighbours, `70/169` / `169/408` algebraic-bound controls, sign orientations, forged metadata, binary float and exact resource refusal.

No numerical minimization, sampling, epsilon, arbitrary denominator cap, arbitrary subdivision depth, timeout or caller certificate becomes truth authority.

## Remaining route

This package qualifies a prerequisite only. It does not promote an event classifier and does not justify an MC-B retry. The next repair is PB-007-01 v33: consume the v32 separator inside the residual v31 projective derivative route, preserve v8-v30 precedence, retain every derivative residual, and discharge the complete pointwise derivative margin with the existing finite orthant / MC-032 exact machinery.
