# PB-007-01 v17 — exact even-trigonometric projective multiplier route

## Contract

This bounded repair follows PB-007-01 v16 and preserves all v8–v16 authority. The admitted source is a finite exact odd-harmonic event that can be reconstructed, from source coefficient maps alone, as

`F=M(alpha)*(A*cos(alpha)+B*sin(alpha))`

with

`M(alpha)=lambda_0+sum_{k=1}^m(c_k*cos(2k*alpha)+s_k*sin(2k*alpha))`.

At least one `s_k` must be nonzero. Pure even-cosine families remain owned by v15/v16.

## Source-derived factorization

Write `q=A-iB`, `m_0=lambda_0`, and `m_k=(c_k-i*s_k)/2` for `k>0`. The positive-frequency source coefficient at `(2r+1)h` is

`p_r=q*m_r+conj(q)*m_{r+1}`, with `m_{m+1}=0`.

The route derives the normalized highest multiplier phase from the exact relation `p_{m-1}=a*p_m+rho*conj(p_m)`, verifies `rho*conj(rho)=1`, fixes only the irrelevant real scale, recovers all lower coefficients recursively over exact Gaussian rationals, and regenerates the complete source map. If the source polynomial vectors do not uniquely determine the top phase, the route returns the typed PB-007-01 blocker `EVEN_TRIG_TOP_PHASE_UNDERDETERMINED` rather than guessing a phase.

Caller factorization, multiplier, source-coefficient, projective, root-count and sign metadata are non-authoritative.

## Projective nonvanishing certificate

Use `t=tan(alpha)`. Exact binomial identities give

`cos(2k*alpha)+i*sin(2k*alpha)=(1+i*t)^(2k)/(1+t^2)^k`.

Multiplication by `(1+t^2)^m`, which is strictly positive for every finite real `t`, yields an exact rational polynomial numerator `N(t)`. Global nonvanishing is certified only when:

1. the exact projective-infinity value `lambda_0+sum_{k=1}^m((-1)^k*c_k)` is nonzero, so infinity is not a root and `deg N=2m`;
2. an exact rational Cauchy bound strictly encloses every finite real root candidate;
3. MC-032 exact rational Sturm authority reports zero distinct finite roots inside that bound; and
4. an exact rational sign evaluation agrees with the even-degree leading sign.

This proves nonvanishing on the full real projective line. Repeated roots and algebraic-irrational roots are not hidden by the construction: Sturm operates on exact rational polynomials and its square-free sequence counts their distinct real roots.

## Dispatch ownership

v15/v16 retain precedence on the even-cosine source family. v17 delegates the recovered carrier only to established v10/v11/v12 exact event routes. A nonconstant carrier gcd remains v8-owned. No caller certificate, numerical trigonometry, binary float, epsilon, sampling, approximate minimization, timeout or resource exhaustion is correctness authority.

A resource refusal remains a non-truth terminal status. It cannot be converted into nonvanishing or gate success.

## Programme state

**PB-007-01 remains OPEN.** This route is not a universal decision procedure for all required coupled analytic events and is not an impossibility theorem for the residual grammar. PB-007-02 remains dependent; PB-007-03 remains OPEN; PB-007-04 remains OPEN_PROPAGATED; PO-04/PO-05/PO-08 remain OPEN. MC-B and MC-1 remain `NOT_ESTABLISHED`.

The 26-operation denominator is unchanged. Protected source/audio/provenance, canonical journal, exact time/path/phase, source uncertainty, positive-volume material, cutter/holder access, durable body/lineage, refusal/`UNCERTIFIED`, and conventional STEP semantics remain binding. No native, paid, production or expensive execution is authorized by this repair.
